import styled from 'styled-components';

export const ButtonWrapper = styled.div`
  position: relative;
  margin: 34px 0;
  cursor: pointer;

  & svg {
    position: absolute;
    top: 0;
    left: 0;
  }
`;
